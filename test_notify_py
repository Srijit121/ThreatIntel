from app.notifications.ntfy import NtfyNotifier

TOPIC = "sriv-threatintel"

notifier = NtfyNotifier(TOPIC)

try:
    notifier.send(
        title="ThreatIntel Test",
        message="This is a direct ntfy test.",
        priority="high",
    )
    print("✅ Notification sent successfully.")
except Exception as e:
    print(f"❌ Error: {e}")
